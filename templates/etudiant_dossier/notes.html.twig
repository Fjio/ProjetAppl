{% extends 'base.html.twig' %}


{% block title %}CINESUP - Etudiant - Mes notes{% endblock %}


{% block body %}
    <div class="container">
        <h1>Mes notes</h1>
    </div>
<div class="row">
    <div class="container col-2" id="nav-eleve">
        <div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist" aria-orientation="vertical">
            <a class="nav-link" id="v-pills-home-tab" data-toggle="pill" href="{{ path('eleve_homepage') }}" role="tab" aria-controls="v-pills-home" aria-selected="false">Accueil</a>
            <a class="nav-link" id="v-pills-home-tab" data-toggle="pill" href="{{ path('etudiant_dossier_administratif') }}" role="tab" aria-controls="v-pills-home" aria-selected="false">Administratif</a>
            <a class="nav-link " id="v-pills-profile-tab" data-toggle="pill" href="{{ path('etudiant_dossier_choixsection') }}" role="tab" aria-controls="v-pills-profile" aria-selected="true">Choix section</a>
            <a class="nav-link" id="v-pills-messages-tab" data-toggle="pill" href="{{ path('etudiant_dossier_scolaire_parcours', {'annee':0}) }}" role="tab" aria-controls="v-pills-messages" aria-selected="false">Parcours scolaire</a>
            <a class="nav-link active" id="v-pills-settings-tab" data-toggle="pill" href="{{ path('etudiant_dossier_scolaire_notes') }}" role="tab" aria-controls="v-pills-settings" aria-selected="false">Notes</a>
            <a class="nav-link" id="v-pills-settings-tab" data-toggle="pill" href="{{ path('etudiant_dossier_validation') }}" role="tab" aria-controls="v-pills-settings" aria-selected="false">Validation</a>
        </div>
    </div>


        <div class="container">
        <table class="table">
            <thead>
            <tr>
                <th>Matière</th>
                <th>Moyenne min de la classe</th>
                <th>Moyenne max de la classe</th>
                <th>Moyenne moyenne de la classe</th>
                <th>Moyenne personnelle</th>
            </tr>
            </thead>
            <tbody>
                {% for form in listeform %}
                    <tr>
                        <td>
                            {{ form[1].nomMatiere }}
                        </td>
                        {{ form_start(form[0], {'attr': {'class': 'form-horizontal'}}) }}
                        <td>
                            {{ form_row(form[0].noteMinClasse) }}

                        </td>
                        <td>
                            {{ form_row(form[0].noteMaxClasse) }}
                        </td>
                        <td>
                            {{ form_row(form[0].noteMoyClasse) }}
                        </td>
                        <td>
                            {{ form_row(form[0].noteEleve) }}
                        </td>
                        <td>
                            {{ form_rest(form[0]) }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>

        </table>
        </div>

</div>


{% endblock %}


